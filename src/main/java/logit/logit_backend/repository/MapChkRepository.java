package logit.logit_backend.repository;

import logit.logit_backend.domain.MapChk;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MapChkRepository extends JpaRepository<MapChk, Long> {
}
